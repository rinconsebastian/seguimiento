
@model IEnumerable<seguimiento.Models.ApplicationUser>
@if (Model.Count() > 0)
{
    <span title="@Model.Count() usuarios">@Model.Count()</span>

}

@*
    @foreach (var item in Model)
    {
        <a class="pill-categoria" href="../Account/Details/@item.Id"> @Html.DisplayFor(modelItem => item.Nombre) </a>


    }

*@